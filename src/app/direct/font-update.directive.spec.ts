import { FontUpdateDirective } from './font-update.directive';

describe('FontUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new FontUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
